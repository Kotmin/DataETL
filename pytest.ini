[pytest]
markers =
    integration: requires running Docker containers and a completed Airflow DAG run
    qa: full pipeline QA — requires running Docker containers; ETL runs are driven by the test itself
